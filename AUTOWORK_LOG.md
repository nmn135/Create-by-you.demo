# AUTOWORK_LOG — 自主工作日志

> **2026-08-13 归档说明**：3D 方案已全部删除，本项目现专注 2D 叙事 demo《第七天》。3D 时期（2026-08-10~08-12）的工作记录已清理。

## 当前项目（2026-08-13）

- **主项目**：`game-prototypes/2d-narrative-demo/`（Canvas 2D + Node + DeepSeek，端口 8890）
- **保留参考**：`text-prototype/`（Python 终端，失言系统/状态机逻辑）
- **已删除**：3D 原型、server.py、3D 资产、3D 文档

## 近期完成（2D 相关）

- 2D demo 3× 分辨率渲染（960×540）修复模糊
- 对话引擎切换 DeepSeek（~3s 端到端，doubao 兜底）
- text-prototype AI 管线模型名修复 + Prompt 模板化
- 测试全绿：状态机 30/30 · 结局 46/46 · 对话 9 轮 0 失言

## 2026-08-13 深夜自主会话

- **修复「游戏坏了」**：根因是场景背景图丢失——`bg.png` 被删、新图误存为 `bd.png`/`bd_day2.png`，游戏加载 404。改名归位 + `loadGame` 旧档残留 bug 修复。
- **第二天场景框架**：SCENES 状态机（day7/day2），正式 `bg_day2.png` 存在时自动替换程序化元素（裂痕/涂鸦/告示回退保留）。
- **上轮记忆回顾**：NPC 对话窗口显示上次最后说的话（存档 + recap 行）。
- **服务端角色锚定**：persona prompt 锚定 + `fixStageDir` 动作主语兜底修正（修好友反馈的"市长抢说书人台词"）。
- **玩法打磨**：气泡超长文字截断不溢出、相邻气泡上下分层不重叠、提高对比度；闲聊触发更快（day1 1.8s/day2 1.0s）、台词扩充。
- **骑砍式话题快捷栏 v1**（节点分支对话）：告别/打听消息/交易/试探刻痕/追问真相，条件解锁；告别关闭对话，其余走 AI 自由对话管线，与输入框并存。
- **对话相机推近**：进入对话平滑推近说话 NPC（zoom 1.3 lerp），结束回全场景。
- **回归测试**：新增 `_harness.js`（DOM/Canvas 桩）**24/24 全绿**。
- **已推送 main**（`39c4307..08f77ec`）。

## 2026-08-13 第二轮自主会话（P5–P11，用户授权"先做吧"）

- **P5 钟楼/门位置参数化**：`BELL_POS`/`DOOR_POS` 常量，钟和门的位置在代码里一处手动调，不动绘制逻辑（用户要手动微调钟的位置）。
- **P6 开场背景故事介绍**：`#intro` 遮罩 + 打字机四行开场文案，点击/ESC 可跳过。
- **P7 NPC 像素肖像 48×48**：对话框头像升级为像素肖像（市长官帽+怀表链 / 当铺老板三副眼镜叠戴 / 说书人半脸涂白+泪痕+丑角帽 / meta 兜帽黑影）；存在 `portrait_<id>.png` 自动覆盖。
- **P8 背景音乐 + 音效**：WebAudio `AudioSys`（程序化 BGM day7/day2 两套循环，文件优先 `audio/bgm_*.ogg`）+ 音效（开/关/发送/气泡/脚步/秘密/结局）；M 键静音并持久化。
- **P9 引导移动 walk 系统**：市长"跟我去钟楼"真的带路（`walkToBell`：市长走向钟楼 + 玩家自动跟随 + 相机跟随），修掉"原地不动"。
- **P10 /api/endgame（server）**：客户端定世界线，LLM 只现写 epilogue；world-state prompt 增加 fragments/inkDone。
- **P11 刻痕三 + 无限结局（client）**：
  - 刻痕三「作者之墨归位」：当铺「买那批话」→ 说书人「归还墨」→ 说书人带路去钟楼归位。
  - meta 终局**四层剧场**：感官（伪终端）→ 叙事（回响玩家过去说过的话）→ 世界（不可能的门）→ 诚实（作者坦白"我改不了任何底层代码"）。
  - **无限结局**：`classifyEnding` 客户端确定性分类世界线（续写/合卷/抹去/坦白之后），LLM 现写 epilogue（离线有 `DEFAULT_EPILOGUE` 兜底）；隐藏结局「坦白之后」在作者坦白之后才解锁。
  - 结局入档、启动页显示「结局已达成·X」、HUD 显示 墨归位/终·X。
  - **resetNewGame**：修复「新的开始」不清空旧循环/结局状态。
- **回归测试**：`_harness.js` 扩到 **39/39 全绿**（新增刻痕三话题/分类/四层剧场/终局/存档往返/重置）。
- **已推送 main**（`08f77ec..8cf0342`）。
