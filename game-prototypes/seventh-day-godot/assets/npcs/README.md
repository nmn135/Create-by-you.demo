# NPC 像素人物

把像素小人 PNG 放这里，**文件名必须等于 NPC 名字**：

- `当铺老板.png`
- `说书人.png`
- `神官.png`
- `市长.png`

放进后，对应 NPC 在世界里就会用这张图（替换原来的色块身体）。
没有图的 NPC 继续用色块身体，不影响游玩。

建议规格（和玩家 `assets/player.png` 一致）：
- 32×32，脚底贴图片底部（脚踩地），居中绘制
- 背景透明（alpha 通道：实体 100%，背景 0%）——注意别像上次 player.png 那样导出成半透明
